{
  "name": "e-commerce",
  "version": "1.0.0",
  "description": "",
  "main": "js/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "esbuild modules/main.js --bundle --outfile=dist/index.js",
    "build:min": "esbuild modules/main.js --bundle --minify --outfile=dist/index.min.js"
  },
  "private": true,
  "devDependencies": {
    "esbuild": "^0.25.4"
  }
}
